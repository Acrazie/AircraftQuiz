---
phase: 05-ux-core-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md
autonomous: true
requirements:
  - UX-01
  - UX-08

must_haves:
  truths:
    - "QuizStandard.jsx hard-coded width fractions (w-1/6, w-4/6, w-2/6) are documented with file:line evidence and severity score"
    - "All page components have their responsive class coverage assessed at 375px and 768px breakpoints"
    - "Missing React Router catch-all route is documented as a finding with file:line evidence"
    - "Nginx SPA routing gap (proxy_pass vs try_files) is documented as a finding"
    - "QuizDebrief.jsx fixed-fraction layout (w-2/5, w-3/5) is assessed at 375px"
  artifacts:
    - path: ".planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md"
      provides: "Responsiveness and routing findings for UX-AUDIT.md"
      contains: "UX-F-"
  key_links:
    - from: ".planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md"
      to: ".planning/UX-AUDIT.md"
      via: "Findings compiled into final audit document in plan 05-03"
      pattern: "UX-F-\\d{3}"
---

<objective>
Audit responsiveness at 375px and 768px breakpoints and verify 404/route fallback coverage. Produce findings file with severity-scored UX findings for QuizStandard hard-coded widths, QuizDebrief layout, QuizVersus layout, leaderboard/profile responsive classes, missing React Router catch-all, and nginx SPA routing gap.

Purpose: Address UX-01 (mobile responsiveness) and UX-08 (404/route fallback) requirements through static code analysis of Tailwind classes and routing configuration.
Output: `findings/05-01-responsiveness-routing.md` containing all UX-F-NNN findings for these two requirements.
</objective>

<execution_context>
@/home/acrasie/.claude/get-shit-done/workflows/execute-plan.md
@/home/acrasie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-core-flows/05-CONTEXT.md
@.planning/phases/05-ux-core-flows/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit responsiveness of all page and quiz components at 375px and 768px</name>
  <files>.planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md</files>
  <read_first>
    client/src/components/quiz/QuizStandard.jsx
    client/src/components/quiz/QuizVersus.jsx
    client/src/components/quiz/QuizDebrief.jsx
    client/src/pages/AirCraftQuiz.jsx
    client/src/pages/Profile.jsx
    client/src/pages/Ranking.jsx
    client/src/pages/Home.jsx
    client/src/pages/Quizzes.jsx
    client/src/pages/Login.jsx
    client/src/pages/Register.jsx
    client/src/layouts/MainLayout.jsx
    client/src/components/ui/TableRank.jsx
    client/src/components/ui/LoginForm.jsx
    client/src/components/ui/RegisterForm.jsx
  </read_first>
  <action>
Read every page component and quiz component listed in read_first. For each file, audit Tailwind responsive classes:

1. **QuizStandard.jsx** (confirmed HIGH finding from research):
   - Document `w-1/6` (sidebar, ~62px at 375px), `w-4/6` (image area), `w-2/6` (answer panel) — all without `sm:` or `md:` responsive variants
   - Document `overflow-y-hidden` on parent that clips content instead of allowing scroll
   - Document `h-1/10` fractional heights on answer buttons that may create tap targets below 44px at 375px
   - Score: HIGH per severity calibration (significantly degrades core quiz flow at 375px)

2. **QuizDebrief.jsx** (research flagged as candidate):
   - Check `w-2/5` and `w-3/5` column splits — at 375px these render as ~150px and ~225px
   - Assess whether score numbers, LP change display, and question review cards overflow or truncate at those widths
   - Check for `flex-col` or responsive stack breakpoint
   - Score based on actual impact assessment

3. **QuizVersus.jsx** (research noted "more responsive"):
   - Verify flex layout has responsive variants
   - Document any fixed widths without breakpoint variants
   - Note if this is CLEAN (no finding needed)

4. **Profile.jsx, Ranking.jsx, Home.jsx, Quizzes.jsx**:
   - For each, grep for `w-` classes without responsive prefixes (sm:, md:, lg:)
   - Check for `min-w-` guards on fractional widths
   - Check for `overflow-hidden` that would clip content at narrow viewports
   - Document any fixed `px` widths or heights without responsive variants
   - Assess podium layout (Ranking.jsx `w-28 md:w-36`) — note this IS responsive

5. **LoginForm.jsx, RegisterForm.jsx, TableRank.jsx**:
   - Check form layout responsive behavior
   - Check table horizontal overflow at 375px (TableRank has 5 columns)

6. **MainLayout.jsx**:
   - Check navbar and footer responsive behavior
   - Check max-width container constraints

For each finding, use this exact format:
```markdown
### UX-F-NNN: [Title]

**Severity:** [CRITICAL|HIGH|MEDIUM|LOW]
**Requirement:** UX-01
**File:** [path]:[line numbers]

**Evidence:**
```jsx
// [3-8 line code snippet]
```

**Impact:** [User friction description at 375px and/or 768px]

**Remediation:** [Specific fix recommendation — e.g., add `flex-col sm:flex-row` or `min-w-[200px]`]
```

Number findings sequentially starting at UX-F-001.
  </action>
  <verify>
    <automated>grep -c "UX-F-" .planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md</automated>
  </verify>
  <acceptance_criteria>
    - findings/05-01-responsiveness-routing.md exists and is non-empty
    - File contains "UX-F-001" (at minimum the QuizStandard finding)
    - File contains "QuizStandard" with "w-1/6" evidence
    - File contains "Severity:" for every UX-F-NNN finding
    - File contains "Requirement: UX-01" on responsiveness findings
    - File contains assessment of QuizDebrief.jsx responsive behavior
    - File contains assessment of Profile.jsx, Ranking.jsx responsive behavior
    - Every finding has Evidence (code snippet), Impact, and Remediation sections
  </acceptance_criteria>
  <done>All page and quiz components assessed for responsive behavior at 375px and 768px; findings documented with file:line evidence, severity scores, and remediation guidance</done>
</task>

<task type="auto">
  <name>Task 2: Audit React Router catch-all and nginx SPA routing fallback</name>
  <files>.planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md</files>
  <read_first>
    client/src/App.jsx
    nginx/nginx.conf
    .planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md
  </read_first>
  <action>
Append routing and fallback findings to the existing findings file from Task 1. Continue the UX-F-NNN numbering from where Task 1 left off.

1. **Missing React Router catch-all** (confirmed HIGH from research):
   - Read App.jsx Routes block
   - Confirm no `<Route path="*">` exists
   - Document the consequence: navigating to `/unknown` renders MainLayout shell with empty `<Outlet>` — no error message, no redirect
   - Score: HIGH (blank content area is significantly confusing; user sees navbar+footer but no content and no indication of error)
   - Cross-refs: Note this was identified as GAP-05 in Phase 1 trust boundary map

2. **Nginx SPA routing gap** (confirmed HIGH from research):
   - Read nginx.conf `location /` block (lines ~128-133)
   - Document `proxy_pass http://frontend:5173` — works in dev because Vite handles all paths
   - Document absence of `try_files $uri /index.html` — in production static deployment, direct URL navigation to any React Router path returns 404 from nginx
   - Score: HIGH (production deployment blocker for SPA deep linking)
   - Note: This is a dev vs production gap — current Docker dev setup works because Vite proxying handles it

3. **Auth redirect without context** (confirmed MEDIUM from research):
   - Read Profile.jsx line 68-70: `if (!isAuthenticated) return <Navigate to="/login" replace />`
   - Document that user lands on login page with no explanation of why they were redirected
   - Check if Login.jsx has any query parameter or state handling for redirect reason — it does not (Login.jsx:9 just redirects authenticated users to /profile)
   - Score: MEDIUM (noticeable usability gap; user may not understand they need to log in to access profile)
   - Requirement: UX-07 (auth flow clarity)

Each finding must use the same format as Task 1 with: UX-F-NNN ID, Severity, Requirement (UX-08 for routing, UX-07 for auth redirect), File:line, Evidence code snippet, Impact narrative, Remediation guidance.

Add a summary section at the end of the file:
```markdown
## Summary

| ID | Severity | Requirement | Description |
|----|----------|-------------|-------------|
| UX-F-001 | ... | UX-01 | ... |
| ... | ... | ... | ... |
```
  </action>
  <verify>
    <automated>grep -c "UX-F-" .planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains a finding for "catch-all" or "path=\"*\"" with Requirement UX-08
    - File contains a finding for "try_files" or "proxy_pass" with Requirement UX-08
    - File contains a finding for "Navigate to=\"/login\"" with Requirement UX-07
    - File contains "## Summary" section with a table listing all findings
    - Each routing finding has Severity, Evidence, Impact, and Remediation sections
    - Auth redirect finding references Profile.jsx:68 or similar line number
    - Nginx finding distinguishes dev (works) from production (broken) context
  </acceptance_criteria>
  <done>React Router catch-all absence, nginx SPA routing gap, and auth redirect clarity all documented as severity-scored findings with evidence and remediation; summary table present</done>
</task>

</tasks>

<verification>
- `findings/05-01-responsiveness-routing.md` exists with at least 4 findings (QuizStandard responsiveness, missing catch-all, nginx SPA gap, auth redirect)
- Every finding has UX-F-NNN ID, Severity, Requirement, File:line, Evidence, Impact, Remediation
- QuizStandard finding scored HIGH with w-1/6, w-4/6, w-2/6 evidence
- Missing catch-all scored HIGH with App.jsx evidence
- Nginx SPA gap scored HIGH with nginx.conf evidence
- Auth redirect scored MEDIUM with Profile.jsx evidence
- Summary table lists all findings
</verification>

<success_criteria>
UX-01 and UX-08 requirements are fully addressed with severity-scored findings; all responsive layout issues and routing fallback gaps are documented with file:line evidence, code snippets, and remediation guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-core-flows/05-01-SUMMARY.md`
</output>
