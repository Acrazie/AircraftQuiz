---
phase: 05-ux-core-flows
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/phases/05-ux-core-flows/findings/05-03-forms-auth.md
  - .planning/UX-AUDIT.md
autonomous: true
requirements:
  - UX-05
  - UX-07

must_haves:
  truths:
    - "LoginForm and RegisterForm page-level alert pattern is documented as MEDIUM finding"
    - "RegisterForm password validator-hint hidden class conflict is documented with evidence"
    - "Profile.jsx silent auth redirect is documented as finding with remediation"
    - "UX-AUDIT.md table-stakes section is complete with all findings from plans 01, 02, and 03"
    - "Every UX-F-NNN finding has consistent format: ID, severity, requirement, file:line, evidence, impact, remediation"
    - "Cross-dimension tags (Cross-refs: SEC-F-NNN) are present where applicable"
  artifacts:
    - path: ".planning/phases/05-ux-core-flows/findings/05-03-forms-auth.md"
      provides: "Form validation and auth flow findings"
      contains: "UX-F-"
    - path: ".planning/UX-AUDIT.md"
      provides: "Complete UX audit document, table-stakes section"
      contains: "UX-F-001"
  key_links:
    - from: ".planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md"
      to: ".planning/UX-AUDIT.md"
      via: "Findings compiled and renumbered sequentially"
      pattern: "UX-F-\\d{3}"
    - from: ".planning/phases/05-ux-core-flows/findings/05-02-loading-error-empty.md"
      to: ".planning/UX-AUDIT.md"
      via: "Findings compiled and renumbered sequentially"
      pattern: "UX-F-\\d{3}"
    - from: ".planning/UX-AUDIT.md"
      to: ".planning/SECURITY-AUDIT.md"
      via: "Cross-refs: SEC-F-NNN tags on relevant findings"
      pattern: "Cross-refs: SEC-F-"
---

<objective>
Audit form validation UX and auth flow clarity, then compile all Phase 5 findings into UX-AUDIT.md table-stakes section. Produce the final deliverable document with sequentially numbered findings, severity distribution, and cross-dimension references.

Purpose: Address UX-05 (form validation UX) and UX-07 (auth flow clarity), then compile UX-AUDIT.md as the phase deliverable document with all findings from plans 01-03 merged and renumbered.
Output: `findings/05-03-forms-auth.md` (intermediate) and `.planning/UX-AUDIT.md` (final deliverable).
</objective>

<execution_context>
@/home/acrasie/.claude/get-shit-done/workflows/execute-plan.md
@/home/acrasie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-core-flows/05-CONTEXT.md
@.planning/phases/05-ux-core-flows/05-RESEARCH.md
@.planning/phases/05-ux-core-flows/05-01-SUMMARY.md
@.planning/phases/05-ux-core-flows/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit form validation UX and auth flow clarity</name>
  <files>.planning/phases/05-ux-core-flows/findings/05-03-forms-auth.md</files>
  <read_first>
    client/src/components/ui/LoginForm.jsx
    client/src/components/ui/RegisterForm.jsx
    client/src/pages/Login.jsx
    client/src/pages/Profile.jsx
    client/src/pages/Register.jsx
    client/src/lib/axios.jsx
    client/src/store/useAuthStore.js
  </read_first>
  <action>
Create findings file for form validation and auth flow issues.

**Form Validation Audit (UX-05):**

1. **LoginForm page-level error alert** (confirmed MEDIUM from research):
   - Read LoginForm.jsx — find the error display pattern
   - Confirm it uses `<div className="alert alert-error">` wrapping all errors at page level
   - Confirm no inline per-field error messages exist (no error text next to email field, no error text next to password field)
   - Document HTML5 attributes present: `required`, `type="email"`, etc. — note these provide browser-native validation but no custom inline error messages
   - Score: MEDIUM (noticeable usability gap per calibration — user must read page-level alert to understand which field failed)

2. **RegisterForm page-level error alert** (confirmed MEDIUM from research):
   - Read RegisterForm.jsx — find the error display pattern
   - Confirm it uses `<div className="alert alert-error">` wrapping errors
   - Document the multi-step wizard pattern as a STRENGTH: validates per step, which is good UX
   - Document HTML5 attributes: `required`, `pattern`, `minLength`, `maxLength`
   - Document DaisyUI `validator-hint` usage: confirm the `input validator` wrapper pattern is used on some fields

3. **RegisterForm password validator-hint hidden** (confirmed MEDIUM from research):
   - Find the password field's `<p className="validator-hint hidden">` element
   - Document the conflict: DaisyUI `validator-hint` is designed to show on `:invalid` state via CSS sibling selector, but Tailwind's `hidden` class sets `display: none` which overrides the DaisyUI CSS
   - The password field is the ONLY field with inline validation guidance potential, but it is permanently suppressed
   - Score: MEDIUM (the one field that has inline guidance has it permanently hidden)

**Auth Flow Clarity Audit (UX-07):**

4. **Silent auth redirect at Profile.jsx** (if not already fully covered in Plan 01):
   - This may have been partially documented in Plan 01 Task 2 — check 05-01-SUMMARY.md
   - If already covered as a finding in Plan 01, add a brief reference here and note it will be compiled into UX-AUDIT.md
   - If not yet covered: document Profile.jsx:68-70 `<Navigate to="/login" replace />` with no state/query parameter explaining the redirect reason
   - Read Login.jsx to confirm no `location.state` message handling exists

5. **Axios 401 interceptor — transparent token refresh UX:**
   - Read axios.jsx interceptor chain — trace the 401 response handler
   - Document whether the token refresh is transparent to the user (no visible loading/spinner during refresh)
   - Document what happens when refresh ALSO fails — does user see an error or just get silently redirected?
   - Note: Session expiry UX is Phase 7 (UX-16) scope — Phase 5 documents the auth flow clarity aspect only
   - Cross-refs: SEC-F-001 (refresh token replay from Phase 2)

6. **ErrorBoundary limited user messaging** (confirm from research):
   - Read ErrorBoundary.jsx — if not already covered in Plan 02
   - This finding spans UX-07 (auth flow clarity extends to general error recovery UX)
   - If covered in Plan 02, reference it here for compilation

Use temporary IDs UX-F-03-001, UX-F-03-002, etc. Same finding format as Plans 01 and 02.

Add summary table at end of file.
  </action>
  <verify>
    <automated>grep -c "UX-F-03-" .planning/phases/05-ux-core-flows/findings/05-03-forms-auth.md</automated>
  </verify>
  <acceptance_criteria>
    - findings/05-03-forms-auth.md exists and is non-empty
    - File contains a finding mentioning "page-level" or "alert-error" for LoginForm with Requirement UX-05
    - File contains a finding mentioning "page-level" or "alert-error" for RegisterForm with Requirement UX-05
    - File contains "validator-hint" and "hidden" in the same finding with Requirement UX-05
    - File contains assessment of axios 401 interceptor behavior
    - Every finding has Severity, Requirement, File:line, Evidence, Impact, Remediation
    - File contains at least 3 UX-F-03-NNN findings
  </acceptance_criteria>
  <done>Form validation UX gaps and auth flow clarity issues documented with severity scores, evidence, and remediation</done>
</task>

<task type="auto">
  <name>Task 2: Compile UX-AUDIT.md table-stakes section from all Phase 5 findings</name>
  <files>.planning/UX-AUDIT.md</files>
  <read_first>
    .planning/phases/05-ux-core-flows/findings/05-01-responsiveness-routing.md
    .planning/phases/05-ux-core-flows/findings/05-02-loading-error-empty.md
    .planning/phases/05-ux-core-flows/findings/05-03-forms-auth.md
    .planning/phases/05-ux-core-flows/05-CONTEXT.md
    .planning/SECURITY-AUDIT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Create `UX-AUDIT.md` in `.planning/` directory. This is the primary Phase 5 deliverable.

**Document structure:**

```markdown
# UX/UI Audit Report — AircraftQuiz

**Audit date:** [current date]
**Auditor:** Static code analysis (no live browser testing)
**Scope:** Phase 5 — Table-stakes UX core flows (UX-01, UX-02, UX-03, UX-05, UX-06, UX-07, UX-08)
**Methodology:** Read components, check Tailwind classes, trace error/loading state coverage per CONTEXT.md locked decisions

---

## Severity Calibration

| Severity | Definition | Example |
|----------|-----------|---------|
| CRITICAL | Completely blocks a core user flow | Quiz unplayable on mobile, login form non-functional |
| HIGH | Significantly degrades a core flow, workaround exists | Hard-coded widths cause truncation at 375px, blank screen on unknown route |
| MEDIUM | Noticeable usability gap, does not block flow | Page-level alerts instead of inline errors, no skeleton screens |
| LOW | Polish issue, minor inconsistency | Empty state wording, minor spacing |

---

## Executive Summary

**Total findings:** [N]
**Severity distribution:** [N] HIGH, [N] MEDIUM, [N] LOW
**Requirements covered:** UX-01, UX-02, UX-03, UX-05, UX-06, UX-07, UX-08

[2-3 sentence summary of key themes: responsiveness gaps in quiz flow, routing fallback missing, form validation uses page-level alerts, loading/error states present but inconsistent]

---

## Findings

[Renumber ALL findings from the three intermediate files into sequential UX-F-001, UX-F-002, UX-F-003... order]

[Sort by severity (HIGH first, then MEDIUM, then LOW)]

[Each finding in this exact format:]

### UX-F-NNN: [Title]

**Severity:** [CRITICAL|HIGH|MEDIUM|LOW]
**Requirement:** [UX-XX]
**File:** [path]:[line numbers]

**Evidence:**
```jsx
// [3-8 line code snippet]
```

**Impact:** [User friction description]

**Remediation:** [Specific fix recommendation]

{If applicable:}
**Cross-refs:** SEC-F-NNN — [brief note on cross-dimension connection]

---

## Coverage Maps

### Loading State Coverage

[Copy the loading/error state coverage map table from Plan 02 findings]

### Empty State Coverage

[Copy the empty state coverage table from Plan 02 findings]

---

## Strengths

[Document positive patterns found during audit:]
- RegisterForm multi-step validation (validates per wizard step)
- Consistent DaisyUI loading-spinner usage across most pages
- Ranking.jsx podium graceful empty slot handling
- [Any other strengths identified during audit]

---

## Requirement Traceability

| Requirement | Findings | Status |
|-------------|----------|--------|
| UX-01 | UX-F-NNN, ... | [N] findings |
| UX-02 | UX-F-NNN, ... | [N] findings |
| UX-03 | UX-F-NNN, ... | [N] findings |
| UX-05 | UX-F-NNN, ... | [N] findings |
| UX-06 | UX-F-NNN, ... | [N] findings / CLEAN |
| UX-07 | UX-F-NNN, ... | [N] findings |
| UX-08 | UX-F-NNN, ... | [N] findings |

---

## Cross-Dimension References

| UX Finding | Security Finding | Connection |
|-----------|-----------------|------------|
| UX-F-NNN | SEC-F-025 | Error leakage UX consequence |
| UX-F-NNN | SEC-F-022 | CSP absence affecting inline styles |
| UX-F-NNN | SEC-F-001 | Session expiry / refresh token UX |

---

## Phase 6-7 Scope Notes

[Brief notes on what Phase 6 (accessibility) and Phase 7 (polish/edge cases) will cover that Phase 5 intentionally excluded:]
- Phase 6: WCAG 2.1 AA automated scan, color contrast, keyboard navigation, screen reader, 3D viewer fallback
- Phase 7: Animation performance, offline behavior, session expiry UX (detailed), notification consistency

---

*Phase: 05-ux-core-flows*
*Status: Table-stakes section complete*
*Next: Phase 6 (Accessibility Audit) and Phase 7 (UX Polish)*
```

Ensure:
- All findings from plans 01, 02, 03 are included — none dropped
- Finding IDs are renumbered to sequential UX-F-001 through UX-F-NNN
- Cross-refs to SEC-F-NNN are preserved from intermediate files
- The document follows the same evidence format as SECURITY-AUDIT.md (file:line + code snippet + impact + remediation)
  </action>
  <verify>
    <automated>grep -c "UX-F-" .planning/UX-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/UX-AUDIT.md exists and is non-empty
    - File contains "# UX/UI Audit Report" header
    - File contains "## Severity Calibration" section
    - File contains "## Executive Summary" with severity distribution count
    - File contains "## Findings" section with UX-F-001 as the first finding
    - File contains "## Loading State Coverage" table
    - File contains "## Empty State Coverage" table
    - File contains "## Requirement Traceability" table with all 7 requirements (UX-01, UX-02, UX-03, UX-05, UX-06, UX-07, UX-08)
    - File contains "## Cross-Dimension References" table with at least SEC-F-025 reference
    - Every UX-F-NNN finding has Severity, Requirement, File:line, Evidence, Impact, Remediation
    - Finding count in UX-AUDIT.md matches total findings across all three intermediate files
    - Findings are sorted by severity (HIGH first)
    - File contains "## Strengths" section
  </acceptance_criteria>
  <done>UX-AUDIT.md table-stakes section is complete with all Phase 5 findings compiled, renumbered sequentially, severity-sorted, with coverage maps, requirement traceability, and cross-dimension references</done>
</task>

</tasks>

<verification>
- `findings/05-03-forms-auth.md` exists with form validation and auth flow findings
- `.planning/UX-AUDIT.md` exists as complete Phase 5 deliverable
- UX-AUDIT.md finding count matches sum of findings from all three intermediate files
- All 7 requirements (UX-01, UX-02, UX-03, UX-05, UX-06, UX-07, UX-08) appear in traceability table
- Cross-dimension references to SECURITY-AUDIT.md are present
- No finding is missing Evidence code snippet
- Executive summary has accurate severity distribution
</verification>

<success_criteria>
UX-05 and UX-07 requirements are addressed; UX-AUDIT.md table-stakes section is the complete Phase 5 deliverable containing all findings from the phase, with consistent formatting, sequential IDs, requirement traceability, and cross-dimension references ready for Phase 10 synthesis.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-core-flows/05-03-SUMMARY.md`
</output>
