---
phase: 01-audit-setup-and-toolchain
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "CONCERNS-TRIAGE.md exists and all 26 CONCERNS.md items appear exactly once in the triage table"
    - "Each concern has a Primary Stream (Security / UX / Maintainability), a Preliminary Severity (CRITICAL/HIGH/MEDIUM/LOW), and a Phase assignment"
    - "Multi-stream concerns have a Secondary Stream column populated (not left blank)"
  artifacts:
    - path: ".planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md"
      provides: "Stream assignment and preliminary severity for all 26 CONCERNS.md items"
      contains: "## Triage Table"
    - path: ".planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md"
      contains: "C-01"
  key_links:
    - from: "CONCERNS-TRIAGE.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "Each row maps one concern to its stream and phase"
      pattern: "GoogleAuthController"
---

<objective>
Triage all 26 items from .planning/codebase/CONCERNS.md into a structured table assigning each concern a primary audit stream, secondary stream (if multi-dimensional), preliminary severity, and the phase that will formally score it.

Purpose: Provides Phase 2–10 with warm starting seeds — each audit stream starts with known issues to confirm and extend rather than a blank slate. The triage table is the Phase 1 deliverable that connects known concerns to the audit plan.
Output: .planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md with all 26 concerns triaged.
</objective>

<execution_context>
@/home/acrasie/.claude/get-shit-done/workflows/execute-plan.md
@/home/acrasie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-setup-and-toolchain/01-RESEARCH.md

<interfaces>
<!-- Pre-populated triage data extracted during research. Use this directly — but read CONCERNS.md first to confirm all 26 items. -->

SEVERITY DEFINITIONS:
- CRITICAL: Exploitable without authentication; data breach or account takeover risk; immediately actionable
- HIGH: Significant security/UX/quality risk; likely exploitable with some effort or visible to users
- MEDIUM: Moderate risk; exploitable under specific conditions or causes degraded UX
- LOW: Minor risk; unlikely to cause immediate harm; quality or polish issue

STREAM DEFINITIONS:
- Security: Vulnerabilities, auth issues, input validation, secrets, CVEs
- UX: User-facing experience gaps, loading/error states, accessibility, responsiveness
- Maintainability: Code quality, test coverage, documentation, architectural fitness, CI/CD

PRE-POPULATED TRIAGE (26 concerns from CONCERNS.md as of 2026-03-21):

Tech Debt (4 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-01 | QuestionFixtures.php 732 lines, hardcoded array | Maintainability | — | MEDIUM | Phase 9 |
| C-02 | Bare catch(\Throwable) in GoogleAuthController line 160 | Security | Maintainability | HIGH | Phase 2 |
| C-03 | Frontend component file sizes (RegisterForm 357L, QuizDebrief 352L, Profile 291L) | Maintainability | — | MEDIUM | Phase 8 |
| C-04 | Loose error handling in axios interceptor (refresh token cleanup) | Maintainability | UX | MEDIUM | Phase 8 |

Known Bugs (3 concerns — note: only 3 exist in CONCERNS.md despite section header saying 4):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-05 | JWT atob() without error handling in useAuthStore.js | Security | UX | MEDIUM | Phase 2 |
| C-06 | Leaderboard cache invalidation race condition (stale 5 min) | UX | Maintainability | LOW | Phase 7 |
| C-07 | Missing division assignment in GoogleAuthController line 86-97 | Security | Maintainability | MEDIUM | Phase 2 |

Security Considerations (6 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-08 | JWT refresh token in localStorage (XSS attack surface, no rotation) | Security | UX | HIGH | Phase 2 |
| C-09 | Google ID token caching without sufficient validation TTL | Security | Maintainability | MEDIUM | Phase 2 |
| C-10 | Avatar upload MIME type validation — polyglot bypass risk | Security | — | MEDIUM | Phase 3 |
| C-11 | SQL injection risk in leaderboard raw query (hardcoded columns) | Security | Maintainability | LOW | Phase 3 |
| C-12 | No rate limiting on /api/questions endpoint | Security | — | HIGH | Phase 4 |
| C-13 | Content-Security-Policy absent from nginx security_headers.conf | Security | — | HIGH | Phase 4 |

Performance Bottlenecks (3 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-14 | Leaderboard query complexity at scale (8-rank CASE, GROUP BY 6) | Maintainability | UX | LOW | Phase 8 |
| C-15 | Answer shuffling in quiz fetch loop | Maintainability | — | LOW | Phase 8 |
| C-16 | Frontend avatar upload synchronous image validation (no progress) | UX | Maintainability | LOW | Phase 5 |

Fragile Areas (4 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-17 | RankingService LP calculation (11 rules, index-based progression) | Maintainability | Security | MEDIUM | Phase 8 |
| C-18 | User entity getter/setter chain (80+ methods, no guards in setters) | Maintainability | Security | MEDIUM | Phase 8 |
| C-19 | GoogleAuthController JWKS retry logic (thrash risk under load) | Security | Maintainability | MEDIUM | Phase 2 |
| C-20 | Zustand auth store persistence middleware (localStorage key hardcoded) | Security | UX | MEDIUM | Phase 7 |

Dependencies at Risk (4 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-21 | firebase/php-jwt v7.0 — JWT standard evolution risk | Security | Maintainability | MEDIUM | Phase 10 |
| C-22 | motion (Framer Motion v12) — large bundle, performance risk | Maintainability | UX | LOW | Phase 10 |
| C-23 | @react-three/fiber v9 — WebGL compatibility surface | UX | Maintainability | MEDIUM | Phase 6 |
| C-24 | aws/aws-sdk-php — large dep tree, upload timeout risk | Maintainability | UX | LOW | Phase 10 |

Missing Critical Features (4 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-25 | No account deletion endpoint (GDPR right-to-be-forgotten) | Security | UX | HIGH | Phase 4 |
| C-26 | No email verification after registration | Security | UX | MEDIUM | Phase 2 |
| C-27 | No password reset flow | UX | Security | MEDIUM | Phase 5 |
| C-28 | No admin dashboard | Maintainability | Security | LOW | Phase 8 |

Test Coverage Gaps (5 concerns):
| C-ID | Concern Title | Primary | Secondary | Severity | Phase |
|------|--------------|---------|-----------|----------|-------|
| C-29 | Frontend integration tests missing (real API calls untested) | Maintainability | — | MEDIUM | Phase 9 |
| C-30 | Profile avatar upload error cases untested | Maintainability | UX | HIGH | Phase 9 |
| C-31 | RankingService master zone boundary tests partial | Maintainability | — | LOW | Phase 9 |
| C-32 | QuestionController daily limit tests missing | Security | Maintainability | HIGH | Phase 9 |
| C-33 | GoogleAuthController token verification edge cases untested | Security | Maintainability | CRITICAL | Phase 9 |

NOTE: The pre-populated data has 29 entries (C-01 to C-33 but some numbered differently). CONCERNS.md organizes into sections with stated totals. When reading CONCERNS.md, count the actual concerns and assign IDs sequentially C-01 through C-N. Adjust IDs in the output table to match what you actually find in the file.

CRITICAL PITFALL: Do not assign Single Stream when Multi-Stream. Every concern involving error handling, exception masking, or race conditions almost always spans dimensions. The "Secondary Stream" column MUST be populated for these — use "—" only when a concern genuinely affects a single dimension.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read CONCERNS.md and produce CONCERNS-TRIAGE.md with all concerns assigned</name>
  <files>.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (authoritative source — read every concern; count them; use actual content, not pre-populated data if different)
  </read_first>
  <action>
    Read .planning/codebase/CONCERNS.md in full. Count the total number of distinct concern items (the research says 26; verify the actual count by reading).

    For each concern found, assign:
    - C-ID: Sequential integer (C-01, C-02, ...)
    - Title: Short descriptive title (from the concern heading in CONCERNS.md)
    - Primary Stream: Security | UX | Maintainability
    - Secondary Stream: Security | UX | Maintainability | — (use "—" only if single-dimension)
    - Preliminary Severity: CRITICAL | HIGH | MEDIUM | LOW
    - Phase: Which phase formally scores this (Phase 2 through Phase 10)

    Use the pre-populated triage data in the interfaces section as a starting point. If the actual CONCERNS.md content matches, use the pre-populated assignments. If CONCERNS.md has different concerns or additional ones, adjust accordingly.

    Then create .planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md with this exact structure:

    ```markdown
    # CONCERNS.md Triage

    **Triaged:** [date]
    **Source:** .planning/codebase/CONCERNS.md
    **Total concerns:** [N]
    **Concerns by stream:** Security: [N], UX: [N], Maintainability: [N]
    **Concerns by severity:** CRITICAL: [N], HIGH: [N], MEDIUM: [N], LOW: [N]

    ## Severity Definitions

    | Level | Definition |
    |---|---|
    | CRITICAL | Exploitable without authentication; data breach or account takeover risk |
    | HIGH | Significant risk; likely exploitable with effort or immediately visible to users |
    | MEDIUM | Moderate risk; exploitable under specific conditions or causes degraded UX |
    | LOW | Minor risk; unlikely to cause immediate harm; quality or polish issue |

    ## Triage Table

    | C-ID | Title | Primary Stream | Secondary Stream | Preliminary Severity | Phase |
    |------|-------|----------------|------------------|----------------------|-------|
    [one row per concern from CONCERNS.md]

    ## Concerns by Section

    ### Tech Debt
    [list C-IDs from this section]

    ### Known Bugs
    [list C-IDs from this section]

    ### Security Considerations
    [list C-IDs from this section]

    ### Performance Bottlenecks
    [list C-IDs from this section]

    ### Fragile Areas
    [list C-IDs from this section]

    ### Scaling Limits
    [list C-IDs from this section — note: these are scaling limits, not audit concerns; assign Maintainability stream, LOW severity unless they have security implications]

    ### Dependencies at Risk
    [list C-IDs from this section]

    ### Missing Critical Features
    [list C-IDs from this section]

    ### Test Coverage Gaps
    [list C-IDs from this section]

    ## Priority Seeds for Each Audit Phase

    ### Phase 2 Seed Concerns (Authentication and JWT Security)
    [List C-IDs assigned to Phase 2 — these are warm starting points for the Phase 2 audit]

    ### Phase 3 Seed Concerns (OWASP and Business Logic)
    [List C-IDs assigned to Phase 3]

    ### Phase 4 Seed Concerns (Infrastructure and Configuration Security)
    [List C-IDs assigned to Phase 4]

    ### Phase 5–7 Seed Concerns (UX Streams)
    [List C-IDs assigned to Phase 5, 6, or 7]

    ### Phase 8–9 Seed Concerns (Maintainability Streams)
    [List C-IDs assigned to Phase 8 or 9]

    ### Phase 10 Seed Concerns (Synthesis)
    [List C-IDs assigned to Phase 10]

    ## Notes

    - Preliminary severity estimates will be formally scored by each stream's audit phase
    - Multi-stream concerns (Secondary Stream != —) will receive cross-dimension tags in Phase 10
    - CRITICAL and HIGH concerns are priority seeds regardless of stream assignment
    ```

    Important severity guidance for multi-stream assignment:
    - Any concern involving exception catching / error masking → always Security + Maintainability
    - Any concern involving auth UX (error messages, redirects) → Security + UX
    - Any concern involving missing features that affect both security and UX (GDPR, email verify) → Security + UX
    - Any concern involving test coverage gaps on security-critical paths → Security + Maintainability
    - Pure code quality/architecture concerns with no security surface → Maintainability alone is acceptable

    For Scaling Limits section concerns: these are architectural capacity concerns, not audit findings. Assign Maintainability as primary, LOW severity, Phase 10. Only escalate if there is an active security implication (there typically isn't for pure scaling concerns).
  </action>
  <verify>
    <automated>test -f /home/acrasie/ProjetsPerso/AircraftQuiz/.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md && grep -q "## Triage Table" /home/acrasie/ProjetsPerso/AircraftQuiz/.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md && grep -q "C-01" /home/acrasie/ProjetsPerso/AircraftQuiz/.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md && grep -q "GoogleAuthController" /home/acrasie/ProjetsPerso/AircraftQuiz/.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md && echo "CONCERNS-TRIAGE.md: OK"</automated>
  </verify>
  <acceptance_criteria>
    - CONCERNS-TRIAGE.md exists at .planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md
    - File contains "## Triage Table"
    - File contains "C-01" (first concern row)
    - File contains "GoogleAuthController" (confirming a key concern is triaged)
    - File header shows correct total concern count
    - Every concern from CONCERNS.md sections appears exactly once in the triage table
    - No concern has blank Primary Stream
    - No concern has blank Preliminary Severity
    - No concern has blank Phase assignment
    - Concerns involving error handling have a non-"—" Secondary Stream entry
    - "## Priority Seeds for Each Audit Phase" section exists with Phase 2 seeds populated
    - CRITICAL-severity concerns (if any) appear under Phase 2 seeds or the phase they belong to
  </acceptance_criteria>
  <done>CONCERNS-TRIAGE.md exists with all CONCERNS.md items triaged; each concern has stream, severity, and phase assignment; priority seeds section enables downstream phases to start with known issues</done>
</task>

</tasks>

<verification>
```bash
TRIAGEFILE="/home/acrasie/ProjetsPerso/AircraftQuiz/.planning/phases/01-audit-setup-and-toolchain/CONCERNS-TRIAGE.md"

# File exists
test -f "$TRIAGEFILE" && echo "File exists: OK"

# Required sections present
grep -q "## Triage Table" "$TRIAGEFILE" && echo "Triage table: OK"
grep -q "## Priority Seeds" "$TRIAGEFILE" && echo "Priority seeds: OK"

# At least one CRITICAL concern (GoogleAuthController edge cases)
grep -q "CRITICAL" "$TRIAGEFILE" && echo "CRITICAL severity present: OK"

# Multi-stream concerns exist (bare catch + jwt storage are known multi-stream)
grep -q "Security.*Maintainability\|Maintainability.*Security" "$TRIAGEFILE" && echo "Multi-stream entries: OK"

# Count concern rows (should be ~26+)
grep -c "| C-" "$TRIAGEFILE" && echo "concern rows (should be 26+)"
```
</verification>

<success_criteria>
- CONCERNS-TRIAGE.md exists with all CONCERNS.md items triaged (minimum 26 rows)
- Every concern has Primary Stream, Secondary Stream (or "—"), Preliminary Severity, and Phase
- Multi-stream concerns are correctly identified (not all assigned single stream)
- Priority seeds section exists and populates each phase with its starting concerns
- The "## Concerns by Section" section preserves the original grouping from CONCERNS.md
- Severity summary header shows counts by CRITICAL/HIGH/MEDIUM/LOW
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-setup-and-toolchain/01-04-SUMMARY.md` with:
- Total concern count (actual, from reading CONCERNS.md)
- Severity distribution: CRITICAL/HIGH/MEDIUM/LOW counts
- Stream distribution: Security/UX/Maintainability counts (by primary stream)
- Multi-stream concern count
- Top 3 highest-severity concerns (with C-IDs and titles)
- Any discrepancies found between pre-populated research data and actual CONCERNS.md content
</output>
